# TSI
TheSourceInstaller a Packet manager which enables building Packets from Source with all its dependencies for Distribution independent use.

## Dependencies

´Bash >= 4.0´

## Install

`/bin/sh tsi/scripts/tsi_installer.sh`